{
    "type": "Lean",
    "sort": "Raw",
    "steps": 5,
    "instruction": "example : ∀ a b c : Nat, a = b → a = c → c = b := by sorry",
    "version": "0.1",
    "initialize": [
        {
            "func": "query",
            "expr": "example : ∀ a b c : Nat, a = b → a = c → c = b := by sorry"
        }
    ],
    "evaluate": [
        {
            "type": "placeholder"
        }
    ]
}
