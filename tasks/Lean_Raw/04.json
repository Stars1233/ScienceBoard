{
    "type": "Lean",
    "sort": "Raw",
    "steps": 10,
    "instruction": "example (a b : ℝ) (h₀ : a ≠ 0 ∧ b ≠ 0) (h₁ : a * b = a - b) : a / b + b / a - a * b = 2 := by sorry",
    "version": "0.1",
    "initialize": [
        {
            "func": "import",
            "libs": ["Mathlib"]
        },
        {
            "func": "open",
            "libs": ["BigOperators", "Real", "Nat"]
        },
        {
            "func": "query",
            "expr": "example (a b : ℝ) (h₀ : a ≠ 0 ∧ b ≠ 0) (h₁ : a * b = a - b) : a / b + b / a - a * b = 2 := by sorry"
        }
    ],
    "evaluate": [
        {
            "type": "placeholder"
        }
    ]
}
