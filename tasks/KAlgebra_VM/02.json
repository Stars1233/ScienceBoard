{
    "type": "KAlgebra",
    "sort": "VM",
    "steps": 5,
    "instruction": "Calculate their distance between points (2, -7) and (7, 5), and store the result in variable `d` in KAlgebra.",
    "version": "0.1",
    "snapshot": "sci_bench",
    "initialize": [
        {
            "func": "launch",
            "wait": 2,
            "command": "LD_LIBRARY_PATH=/home/user/6.5.0/gcc_64/lib /app/bin/kalgebra «PORTLIKE»",
            "shell": true
        }
    ],
    "evaluate": [
        {
            "type": "vars",
            "key": "d",
            "value": 13
        }
    ]
}
